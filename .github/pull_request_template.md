## Summary

<!-- What does this PR do, and why? -->

## Parity

The Android and iOS SDKs implement one shared contract — see [`SDK_PARITY.md`](../SDK_PARITY.md)
(canonical spec: `android-sdk/SDK_PARITY_SPEC.md`).

- [ ] This PR changes the **public API surface** or **storage contract** (spec §3 / §4).
  - If checked:
    - [ ] The `parity:needed` label is applied to this PR.
    - [ ] The parity spec is updated (new field/method, or a new §5 accepted divergence).
    - [ ] A counterpart issue exists in `android-sdk` — or will be auto-created on merge by the parity-mirror workflow.
- [ ] This PR is platform-only (build, CI, docs) and not parity-relevant.

## Testing

<!-- How was this verified? Unit tests, integration tests, manual device check. -->
